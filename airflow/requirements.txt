# Extra dependencies not included in Cloud Composer
# During local dev & testing, you can `pip install -e ../cli` first
# pip will then prioritise the local solana-etl package over pypi

discord-webhook==0.16.3
solana-etl==0.0.6
